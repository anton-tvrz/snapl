Intent store maps the real `management_ip` schema attribute (stringified, CIDR prefix stripped) instead of the nonexistent `management_address`; `Device` carries `lab_node_name` and the dcfabric seed populates it — live Infrahub reads no longer return empty management addresses.
