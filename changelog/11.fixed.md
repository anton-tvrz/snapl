Fix `ReconcileDevicesWorkflow` crash-looping its workflow task: it passed `id_conflict_policy` to `execute_child_workflow()`, which only accepts that parameter on client `start_workflow` calls. The invalid kwarg is removed; the deterministic child workflow id already deduplicates within a run.
