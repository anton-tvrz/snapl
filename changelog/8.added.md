Add NAF Observability building block — `StructuralObserver` for drift detection, `EventBus` for in-process event dispatch, and `AuditLog` for queryable in-memory audit history. Async event bus and durable audit storage are deferred to the Orchestrator block.
