The orchestrator live integration suite is real: SC-001 (deploy < 60s on real Temporal), SC-003 (worker restart resumes without work loss), SC-005 (audit events survive worker restart), and SC-007 (induced drift on a live SR Linux node is detected by scan and healed by reconcile) now execute against the running stack and skip cleanly when it is absent.
