Implement `snapl-executor` package — GnmiExecutor with apply, dry_run, rollback, apply_batch; SR Linux YANG JSON rendering via Jinja2 templates; structured result objects (ApplyResult, DryRunResult, BatchResult); no exceptions for device-side errors.
