The `dcfabric` Containerlab topology (`containerlab/dcfabric.yml`) is now committed: 2 spines / 4 leaves of Nokia SR Linux with node names, device types, and cabling matching the SoT seed data, plaintext gNMI on 57400, and a README covering deploy/destroy and credentials. `invoke dev.lab-deploy` / `dev.lab-destroy` drive it without a native containerlab install (dockerized clab).
