Executor and collector resolve the gNMI dial target per call from the device (`lab_node_name`, then `management_address`, then optional constructor fallback); the Temporal worker no longer pins a `host="placeholder"` instance, so live workflows reach real devices. gNMI sets serialize per dial host.
