New opt-in end-to-end suite (`uv run invoke test-e2e`, or `SNAPL_E2E=1 pytest tests/e2e -m e2e`) asserts the whole demo arc against a live stack: provision the schema, seed, verify every device's dial target matches the lab's static pins and is actually reachable, deploy, inject real drift over raw gNMI, scan and assert the exact drifted path, reconcile, rescan clean, then read back the audit trail. Its value over the existing integration suite is the prefix that suite skips — bootstrap — which is where every recent demo break actually lived (#87, #78, #90, #96). It refuses to run against an Infrahub that has devices but no snapl `use_case` attribute, so it cannot seed into a neighbouring project's Source of Truth (#107).
