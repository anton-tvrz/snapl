`get_desired_state` now assembles interfaces and BGP sessions from child-side queries (`InterfacePhysical`/`RoutingBGPSession` by `device__ids`, with prefetched relations) and maps the real Infrahub shapes — IPs from the `ip_addresses` relation's CIDR, ASNs from `local_as`/`remote_as` peers. Live desired state is no longer empty of config intent.
