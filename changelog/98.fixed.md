`docs/demo-scenarios.md` is runnable as written again: the snippets no longer mix Infrahub port 8001 with Temporal port 7233, setup uses the new `invoke demo.up` instead of pasting async Python into a REPL, the Temporal client is built via `snapl_orchestrator.worker.client.build_client` so the pydantic data converter is wired in, and the claim about which scenarios work without a lab is now accurate (only 5 and 8). Adds sections on resetting between rehearsals and on the known convergence limitations worth naming up front.
