Intent mapping hardening: `get_desired_state` now picks a dual-stack interface address deterministically (prefer IPv4) instead of taking whichever `ip_addresses` peer the SDK ordered first, and `_peer_id` stringifies the peer's id so a `uuid.UUID` object no longer drops the child from its device group or raises in `UUID(...)`.
