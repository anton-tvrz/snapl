BGP `router-id` now derives from the device's loopback address (the seed designates lo0's IP as the router ID) instead of the hardcoded `management_address`, falling back to management only when no loopback in intent carries an address. Rendered config and seeded intent agree.
