The structural diff no longer phantom-drifts on an mtu-less ethernet interface: a per-field `skip_when_none` in `ENTITY_FIELD_MAP` treats an unset mtu in intent as "not managed", so the operational mtu a device reports on its own is not flagged as drift on every scan. A set mtu is still compared strictly.
