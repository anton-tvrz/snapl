`get_desired_state` no longer crashes when an interface's `ip_addresses` relation peer isn't in the SDK store: the cardinality-many peer is now resolved through the same guarded helper as every other relation, degrading to `ip_address=None` instead of propagating the live SDK's raising `.peer` property.
