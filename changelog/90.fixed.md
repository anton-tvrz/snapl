Isolated the dcfabric lab onto a dedicated management network (`clab-snapl` / `172.20.21.0/24`) with static per-node IP pins (.11–.16). Previously the lab inherited containerlab's default `clab` / `172.20.20.0/24` bridge, shared with other labs on the same host, so nodes from different projects interleaved on one network and a workflow could reach a foreign project's SR Linux nodes.
