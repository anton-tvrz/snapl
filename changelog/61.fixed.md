Worker bootstrap config: `INFRAHUB_ADDRESS` now resolves through the intent client's single default (no more divergent 8001), `SRLINUX_PORT` and `SRLINUX_INSECURE` are honored (previously documented but ignored/hardcoded).
