`uv run invoke dev.deps` now exists and brings up the full dev dependency stack — Infrahub, its backing stores, and a Temporal dev server (frontend :7233, Web UI :8233) — with a single command, waiting for health. `dev.stop` and `dev.down [--volumes]` are the counterparts. Quickstarts and demo scenarios updated to match.
