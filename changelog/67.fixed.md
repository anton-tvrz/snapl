The Temporal worker's drift observer no longer leaks memory and its event path is live: the worker wires a capacity-bounded observer audit log (the orchestrator's durable audit log remains the single sink), every completed drift check emits an ObservabilityEvent (FR-004), and the worker registers a structured-log handler so drift/error events surface in worker logs.
