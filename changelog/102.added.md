Added `specs/006-presentation-cli/spec.md`, the specification for the Presentation block. Presentation was the one NAF block without a spec; this closes that gap and records the decisions the CLI rests on — the command surface, the boundary against `invoke` (snapl operates the network, invoke operates the developer environment), and the exit-code contract in which drift is 2 rather than 0 or 1.
