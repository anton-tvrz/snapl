`SqliteAuditLog(database_url=":memory:")` works as documented: a persistent connection backs memory mode instead of a fresh empty database per operation (previously every append failed with "no such table").
