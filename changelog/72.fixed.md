Rendering an interface that has an IP address but no prefix length is now a render error reported by `dry_run` and raised by `apply`, instead of pushing an invalid `x.x.x.x/None` prefix the device rejects with an opaque error.
