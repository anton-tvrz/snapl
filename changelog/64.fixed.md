`interfaces.j2` now renders every intent interface: IP-less interfaces get their `admin-state` (and MTU/description) pushed instead of being silently skipped; the IP subinterface block is rendered only when an address is present.
