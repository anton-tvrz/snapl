The structural diff no longer compares fields the executor never configures: interface `description` and `mtu` are now rendered, and the unenforceable device-description comparison at `/system` was removed — deploys with SoT-sourced intent can verify CLEAN instead of failing on permanent phantom drift. A render→normalize→diff contract test keeps the renderer and diff from silently diverging again.
